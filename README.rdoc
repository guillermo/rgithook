= RGitHook

RGitHook is a simple but powerfull hook manager for git repository.
It record hooks in a single file in GIT_DIR/hooks/rgithook and config hooks to use rgithook

WARNING: Any existing hook will be overwrited

To use just tape in your repo

  rgithook --install

To start editing your hooks

rgithook --edit

= Install

